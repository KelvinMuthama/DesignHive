import { GraphQLClient } from "graphql-request";

const makeGraphQLRequest = async (query: string, variables = {}) => {
  try {
    //client.request....
    
  } catch (error) {
    throw error;
  }
};
